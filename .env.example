TURSO_DB_URL=libsql://<your-db>-<org>.turso.io
TURSO_DB_AUTH_TOKEN=<your-auth-token>

# JWT signing secret — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Required for persistent sessions across deploys.
SIGNOUTH_SECRET=
