# DEV-114 & DEV-119 — Refactor Monolith & Skeleton Loading

> **Execution:** Claude Code → Codex CLI → DeepSeek (fallback)

**Goal:** Refactor `components/sewain-app.tsx` (2880 lines) into modular component files, then add skeleton loading states.

**Architecture:** Pure extraction with zero logic changes. Each exported piece maps 1:1 to the monolith's existing code. After refactor, `sewain-app.tsx` becomes an orchestrator that imports and wires everything together.

**Tech Stack:** Next.js 15, pnpm, TypeScript, React

---

## Phase 1: DEV-114 — Refactor Monolith

### Task 1: Context Providers

**Objective:** Extract I18n, Token, Access contexts into separate files

**Files:**
- Create: `components/context/i18n-context.tsx`
- Create: `components/context/token-context.tsx`
- Create: `components/context/access-context.tsx`
- Create: `components/context/index.ts`
- Modify: `components/sewain-app.tsx` (replace inline context with imports)

**Extract from sewain-app.tsx (line ranges):**
- I18nContext/useI18n: lines 51-53
- TokenConfigContext/useTokenConfig: lines 55-57
- AccessContext/useAccess: lines 59-69

**Step 1:** Create `i18n-context.tsx` — copy type `I18nState`, `I18nContext`, `useI18n` + needed imports
**Step 2:** Create `token-context.tsx` — copy `TokenCtx`, `TokenConfigContext`, `useTokenConfig`
**Step 3:** Create `access-context.tsx` — copy `AccessCtx`, `AccessContext`, `useAccess`
**Step 4:** Create `context/index.ts` — re-export all
**Step 5:** Replace inline code in sewain-app.tsx with imports from `@/components/context`

---

### Task 2: Layout Components

**Objective:** Extract Shell, Sidebar, Topbar, Notifications/Dropdowns

**Files:**
- Create: `components/layout/shell.tsx`
- Create: `components/layout/sidebar.tsx`
- Create: `components/layout/topbar.tsx`
- Create: `components/layout/notifications.tsx`
- Create: `components/layout/index.ts`
- Modify: `components/sewain-app.tsx`

**Extract from sewain-app.tsx:**
- Sidebar: lines ~2830-2850 (brand, nav, language-switcher, profile)
- Topbar: line ~2850+ (search, actions, notifs, acting-as)
- Notification panel: lines ~2850+ (notification-anchor, popover)
- Shell wrapper: the outer div structure

**Step 1:** Create layout files — extract JSX + any related state handlers
**Step 2:** Update sewer-app.tsx to import and use them

---

### Task 3: Page Components

**Objective:** Extract each of 12 pages into own file

**Files:**
- Create: `components/pages/dashboard.tsx`
- Create: `components/pages/calendar-page.tsx`
- Create: `components/pages/properties-page.tsx`
- Create: `components/pages/tenants-page.tsx`
- Create: `components/pages/reservations-page.tsx`
- Create: `components/pages/invoices-page.tsx`
- Create: `components/pages/tokens-page.tsx`
- Create: `components/pages/contracts-page.tsx`
- Create: `components/pages/messages-page.tsx`
- Create: `components/pages/tickets-page.tsx`
- Create: `components/pages/documents-page.tsx`
- Create: `components/pages/settings-page.tsx`
- Create: `components/pages/index.ts`

**Extract from sewain-app.tsx (approximate line ranges):**
- Dashboard: `{page === "dashboard" && <Dashboard ... />}` section
- Each page follows same pattern in the main render block

**Step 1:** For each page, extract the component function + its sub-components into its own file
**Step 2:** Update main render in sewain-app.tsx

---

### Task 4: Dialog Components

**Objective:** Extract Create/Edit dialogs

**Files:**
- Create: `components/dialogs/create-edit-dialog.tsx`
- Create: `components/dialogs/index.ts`
- Modify: `components/sewain-app.tsx`

**Extract:** The `save`, `dialog` rendering logic, form schemas

---

### Task 5: Re-integrate Orchestrator

**Objective:** `sewain-app.tsx` becomes a thin orchestrator

**Files:**
- Modify: `components/sewain-app.tsx` (import all new components, remove inline code)

**Step 1:** Replace all inline component definitions with imports
**Step 2:** Verify `pnpm run dev` / `pnpm run build` passes
**Step 3:** Commit

---

## Phase 2: DEV-119 — Skeleton Loading

### Task 6: Add Skeleton Loading State

**Objective:** Add `loading` state to `useStoredRows` and render skeleton placeholders

**Files:**
- Modify: `components/sewain-app.tsx` or create `components/skeleton.tsx`
- Add loading state to `useStoredRows` hook (adds 3s loading simulation for initial load)

**Step 1:** Create `SkeletonRow` component
**Step 2:** Modify `useStoredRows` to expose `loading` state
**Step 3:** Add conditional skeleton rendering in each page component

---

## Execution Order

1. Context providers (foundation — everything depends on them)
2. Layout components (Shell, Sidebar, Topbar)
3. Page components (12 files, parallel-friendly)
4. Dialog components
5. Re-integrate orchestrator
6. Skeleton loading
7. Final verification: build + visual check on `sewain-ochre.vercel.app`

## Verification Steps

```bash
cd ~/projects/sewain
pnpm run build   # must pass with zero errors
pnpm run dev     # must start without runtime errors
```

## Risks & Notes

- **DO NOT change any logic** — extraction only, pure refactor
- Some components (like Dashboard inline in `{page === ...}` blocks) use closures over parent state — may need to pass props
- `useStoredRows`, `useStoredConfig`, `useStoredState`, and all utility functions stay in `sewain-app.tsx` (or move to `lib/`)
- Line numbers may shift during refactor — verify after each extraction
